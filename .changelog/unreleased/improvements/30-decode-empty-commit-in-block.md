- `[cometbft]` Allow misformed blocks (eg. with empty `last_commit`
  on non-first block) when decoding them from Protobuf or RPC responses
  ([\#30](https://github.com/cometbft/cometbft-rs/issues/30))
